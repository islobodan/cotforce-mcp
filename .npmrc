# Enforce engine requirements (Node >= 18)
engine-strict=true

# Use exact versions instead of semver ranges when adding deps
save-exact=false

# Always generate/update lockfile
package-lock=true

# Suppress funding messages in CI
fund=false

# Registry (default npmjs)
registry=https://registry.npmjs.org/
